<Page
    x:Class="Commander_Scoreboard.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Commander_Scoreboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="PlayerNameStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="72"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="Orange"/>
        </Style>
        <Style x:Key="PoisonCounterStyle" TargetType="TextBlock" BasedOn="{StaticResource PlayerNameStyle}">
            <Setter Property="Foreground" Value="GreenYellow" />
            <Setter Property="FontSize" Value="54"/>
        </Style>
        <Style x:Key="CommanderCostStyle" TargetType="TextBlock" BasedOn="{StaticResource PoisonCounterStyle}">
            <Setter Property="Foreground" Value="Gold"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="54" />
            <Setter Property="Width" Value="100" />
            <Setter Property="Foreground" Value="Orange"/>
        </Style>
        <Style TargetType="Border">
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Margin" Value="15"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="Foreground" Value="DarkOrange"/>
        </Style>
    </Page.Resources>


    <Page.DataContext>
        <local:Game />
    </Page.DataContext>

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GridView ItemsSource="{Binding Players}" SelectionMode="Single" SelectedItem="{Binding CurrentPlayer}" Grid.Row="0">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Viewbox  StretchDirection="DownOnly">
                            <TextBlock Text="{Binding Name}" Style="{StaticResource PlayerNameStyle}" />
                        </Viewbox>
                        <TextBlock Text="{Binding Life}" Style="{StaticResource PlayerNameStyle}" />
                        <TextBlock Text="{Binding Poison}" Style="{StaticResource PoisonCounterStyle}" />
                        <TextBlock Text="{Binding CommanderAdditionalCost}" Style="{StaticResource CommanderCostStyle}" />
                        <ListView ItemsSource="{Binding CommanderDamage}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Key.Name}" />
                                        <TextBlock Text="{Binding Value}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <AppBar Grid.Row="1" IsSticky="True" IsOpen="{Binding ShowCommands}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Empty Space -->
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}">Normal Damage</TextBlock>
                <Border Grid.Column="0" Grid.Row="1">
                    <StackPanel Orientation="Horizontal">
                        <Button>-5</Button>
                        <Button>-1</Button>
                        <Button Command="{Binding Plus1Life}">+1</Button>
                        <Button>+5</Button>
                    </StackPanel>
                </Border>
                <TextBlock Grid.Column="1" Grid.Row="0" Style="{StaticResource LabelStyle}">Poison Damage</TextBlock>
                <Border Grid.Column="1" Grid.Row="1">
                    <StackPanel Orientation="Horizontal">
                        <Button>
                            <TextBlock Style="{StaticResource PoisonCounterStyle}">-1</TextBlock>
                        </Button>
                        <Button>
                            <TextBlock Style="{StaticResource PoisonCounterStyle}">+1</TextBlock>
                        </Button>
                    </StackPanel>
                </Border>
                <TextBlock Grid.Column="2" Grid.Row="0" Style="{StaticResource LabelStyle}">Recast Cost</TextBlock>
                <Border Grid.Column="2" Grid.Row="1">
                    <StackPanel Orientation="Horizontal">
                        <Button Foreground="Gold">-1</Button>
                        <Button Foreground="Gold">+1</Button>
                    </StackPanel>
                </Border>
                <TextBlock Grid.Column="4" Grid.Row="0" Style="{StaticResource LabelStyle}">Commander Damage</TextBlock>
                <Border Grid.Column="4" Grid.Row="1"> 
                    <StackPanel Orientation="Horizontal">
                        <ComboBox ItemsSource="{Binding Players}" SelectedItem="{Binding CommanderDamageSource}" DisplayMemberPath="Name"/>
                        <Button>-1</Button>
                        <Button>+1</Button>
                    </StackPanel>
                </Border>
            </Grid>
        </AppBar>
    </Grid>
</Page>
